meta {
  name: login
  type: http
  seq: 6
}

post {
  url: {{webBaseUrl}}/login
  body: json
  auth: none
}

headers {
  X-XSRF-TOKEN: {{X-XSRF-TOKEN}}
  Accept: application/json
  Referer: http://localhost:8000
}

body:json {
  {
    "email": "john.doe@example.com",
    "password": "password"
  }
}

script:pre-request {
  const token = req.headers;
  console.log('here666', token);
}
